// Use 'git update-index --assume-unchanged src/main/config/keys.js' to prevent changes being seen (and accidentally committed)
// Use 'git update-index --no-assume-unchanged src/main/config/keys.js' to reset this if required

'use strict';

module.exports = {
    app_id : 'APP_ID',
    js : 'KS_KEY'
};